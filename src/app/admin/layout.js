import "../globals.css";

export const metadata = {
  title: "Admin Dashboard — Noel Siby Portfolio",
  description: "Edit portfolio content: bio, projects, skills, and social links.",
};

export default function AdminLayout({ children }) {
  return children;
}
